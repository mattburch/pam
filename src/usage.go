package main

const usage = `

Usage:
  pam [--bind_ip=<ip> --bind_port=<port>]

Options:
  -h, --help              Show usage.
  --version               Show version.
  --bind_ip=<ip>          Local IP address to bind PAM to [default: 127.0.0.1].
  --bind_port=<port>      Local Port to bind PAM to [default: 3000].
`
